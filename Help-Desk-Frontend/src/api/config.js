// API Configuration
// Centralized configuration for API endpoints

// Use relative path for proxy to work in development
// In production, this might need to be an absolute URL or managed via env variables
export const API_BASE_URL = '/api';
export const STORAGE_BASE_URL = '/storage';
